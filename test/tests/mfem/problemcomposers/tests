[Tests]
  design = 'MFEMProblemComposer.md'
  issues = '#33021'
  [WeakFormProblemComposer]
    type = CSVDiff
    input = explicit_composer_darcy.i
    csvdiff = 'OutputData/Darcy.csv'
    requirement = 'The system shall allow the user to explicitly build an MFEMWeakFormProblemComposer '
                  'while still getting the same results for, e.g., a Darcy problem.'
    capabilities = 'mfem'
    compute_devices = 'cpu cuda'
    recover = false
  []
  [TimeDependentWeakFormProblemComposer]
    type = XMLDiff
    input = explicit_composer_heat_transfer.i
    xmldiff = 'OutputData/HeatTransfer/Run0/Run0.pvd
                OutputData/HeatTransfer/Run0/Cycle000003/proc000000.vtu'
    requirement = 'The system shall allow the user to explicitly build an MFEMTimeDependentWeakFormProblemComposer '
                  'while still getting the same results for, e.g., a heat transfer problem.'
    capabilities = 'mfem'
    compute_devices = 'cpu cuda'
    max_parallel = 1 # schemadiff with multiple ranks
    recover = false
    restep = false
  []
  [CustomProblemComposer]
    type = CSVDiff
    input = custom_composer_and_operator.i
    csvdiff = 'OutputData/custom_composer_and_operator/l2norm.csv'
    requirement = 'The system shall have the ability to use a custom operator based on MFEM ex0p'
    capabilities = 'mfem'
    compute_devices = 'cpu'
    max_parallel = 1 # schemadiff with multiple ranks
    recover = false
    restep = false
  []
  [RogersRicci2D]
    type = CSVDiff
    input = rogers_ricci_2d.i
    cli_args = 'Executioner/num_steps=5'
    csvdiff = 'OutputData/rogers_ricci_2d.csv'
    requirement = 'The system shall be able to solve the 2D Rogers-Ricci drift-turbulence model '
                  'using a custom time dependent problem operator that advances temperature, '
                  'vorticity, density and electrostatic potential as a single implicit block '
                  'system, with the potential constrained by a Poisson equation rather than '
                  'solved separately.'
    capabilities = 'mfem'
    compute_devices = 'cpu'
    recover = false
  []
[]
